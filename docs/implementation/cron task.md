Combodo's documentation gives an example where you create a task or cron job to run every five minutes. 
However, the process runs way more often. That's why periodicity of Mail to Ticket Automation is set to 30 seconds by default and works.

It's just a fallback in case something went wrong.